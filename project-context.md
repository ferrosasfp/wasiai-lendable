# project-context.md — wasiai-lendable (Cobraya)

> Auto-generated by nexus-analyst F0 on 2026-05-15 during WKH-COBRAYA-AGENTS.
> Source: package.json + scaffold inspection + BACKLOG.md + TRANSLATION-MATRIX.md.

---

## Proyecto

**Nombre**: Cobraya
**Tagline**: "¿Ya cobraste? Cobraya."
**Repo GitHub**: github.com/ferrosasfp/wasiai-cobraya (filesystem: `wasiai-lendable`)
**Descripción**: Factoring agéntico para PyMEs mexicanas. Lupita sube CFDI → 4 agentes IA procesan en <60s → recibe USDC en wallet. Settlement en Avalanche Fuji.
**Puerto local**: 3010 (configurado en package.json dev/start scripts)
**Hackathon**: Avalanche LATAM Fintech Build · 15-17 mayo 2026
**Metodología**: NexusAgil QUALITY AUTO — pipeline completo en una HU `WKH-COBRAYA-AGENTS`

---

## Stack técnico

### Frontend / Full-stack
| Herramienta | Versión | Rol |
|---|---|---|
| Next.js App Router | 14.2.5 | Framework full-stack (UI + API Routes) |
| React | 18.3.1 | UI |
| TypeScript | 5.5.3 | strict — sin `any` explícito |
| Tailwind CSS | 3.4.6 | Styling |
| viem | 2.21.0 | EIP-3009 typed data signing + Avalanche RPC |
| zod | 3.23.8 | Env schema validation |

### PWA (a instalar en W0.5)
| Herramienta | Versión | Rol |
|---|---|---|
| @ducanh2912/next-pwa | ^10.2.9 | Service worker + manifest (patrón luma-ai) |
| workbox-* | transitivo | Runtime caching strategies |

### Smart Contracts (a crear en W2.5)
| Herramienta | Versión | Rol |
|---|---|---|
| Foundry (forge) | latest | Build + test + deploy |
| Solidity | 0.8.24 | `CobrayaInvoiceCommitments.sol` |
| OpenZeppelin | lib/openzeppelin-contracts | Ownable2Step + custom errors |

### Blockchain / Settlement
| Componente | Valor |
|---|---|
| Chain demo | Avalanche Fuji (chainId 43113) |
| Chain prod-ready | Avalanche Mainnet (43114) |
| Asset | USDC (Fuji mock: `0x5425890298aed601595a70AB815c96711a31Bc65`) |
| Settlement infra | wasiai-facilitator (Railway prod) |
| Discovery/compose | wasiai-a2a (Railway prod, multi-chain) |

### Sistemas externos consumidos (NO modificar)
| Sistema | URL | Rol |
|---|---|---|
| wasiai-a2a | https://wasiai-a2a-production.up.railway.app | /discover + /compose (debita budget) |
| wasiai-facilitator | https://wasiai-facilitator-production.up.railway.app | /settle (EIP-3009 submit) |
| wasiai-v2 | marketplace prod | agentes registrados vía Supabase SQL |
| Anthropic Claude API | api.anthropic.com/v1/messages | rationale del credit-scorer (opcional con fallback) |
| Avalanche Fuji RPC | https://api.avax-test.network/ext/bc/C/rpc | contract reads/writes |
| Snowtrace | testnet.snowtrace.io | explorer + contract verification |

---

## Arquitectura de capas (hexagonal-light)

```
src/
├── core/          reglas puras: scoring bands, lender catalog, fee math, mxn↔usdc
├── infra/         I/O outbound: a2a-client, facilitator-client, llm-client, mock-adapter, eip3009-signer
├── application/   use cases: validate-invoice, score-invoice, match-lender, settle-factoring
├── app/           Next.js App Router (pages + API routes + PWA shell)
├── components/    React UI
│   └── pwa/       PWA-specific components (register-sw, install-prompt)
├── types/         DTOs compartidos
└── lib/           utilities (mock-data, abis)

contracts/         Foundry project (creado en W2.5)
├── src/           CobrayaInvoiceCommitments.sol
├── test/          .t.sol unit tests
└── script/        Deploy.s.sol
```

**Regla de dependencia**: `app → application → infra → core`. `core` no importa de ningún otro layer.

---

## Wallets (testnet, todas en .env.local — gitignored)

| Rol | Dirección | Propósito |
|---|---|---|
| OWNER | `0x94DCDb...` | SME — recibe USDC del settlement |
| TREASURY | `0x1d024B...` | Lender — firma EIP-3009, fondos USDC, deploy contract |
| OPERATOR | `0xf432ba...` | wasiai-a2a gateway — NO se modifica |

**A2A_KEY Cobraya**: `wasi_a2a_84d1db...` · key_id `795415ba-fe51-43d1-ae40-ce287cd1e233`

---

## Env vars relevantes

| Variable | Descripción | Obligatoria |
|---|---|---|
| `NEXT_PUBLIC_DEMO_MODE` | `true` = mocks deterministas, sin red | Sí (paracaídas) |
| `WASIAI_A2A_URL` | URL wasiai-a2a | Sí |
| `WASIAI_FACILITATOR_URL` | URL facilitator | Sí |
| `A2A_KEY` | API key Cobraya | Sí |
| `AVALANCHE_CHAIN_ID` | 43113 (Fuji) | Sí |
| `USDC_ADDRESS` | USDC contract en chain | Sí |
| `TREASURY_PRIVATE_KEY` | Server-side EIP-3009 signer | Sí (prod) |
| `OWNER_ADDRESS` | Receiver del settlement | Sí |
| `COBRAYA_COMMITMENTS_ADDRESS` | Contract fraud-detector (post-deploy W2.5) | Sí |
| `FRAUD_DETECTOR_PRIVATE_KEY` | Hot key para writes en el contract | Sí |
| `ONCHAIN_AMOUNT_CAP_USDC` | Cap testnet (default 0.05) | Sí |
| `ANTHROPIC_API_KEY` | Claude API para rationale (opcional) | No — fallback local |
| `VALIDATOR_HOT_KEY` | EIP-712 signer cfdi-validator | Sí (audit trail) |
| `FRAUD_HOT_KEY` | EIP-712 signer fraud-detector | Sí (audit trail) |
| `SCORER_HOT_KEY` | EIP-712 signer credit-scorer | Sí (audit trail) |
| `MATCHER_HOT_KEY` | EIP-712 signer lender-matcher | Sí (audit trail) |
| `SNOWTRACE_API_KEY` | Para forge verify automated | Sí (deploy) |
| `DEPLOYER_PRIVATE_KEY` | TREASURY key en formato Foundry | Sí (deploy) |
| `FRAUD_DETECTOR_AGENT_WALLET` | Hot key addr para initialCommitter del contract | Sí (deploy) |

---

## Scaffold ya existente (pre-hack)

| Archivo | Estado |
|---|---|
| `src/types/invoice.ts` | Existe — necesita extensión con auction fields |
| `src/components/PipelineProgress.tsx` | Existe — necesita adaptación a 4 agentes + mobile |
| `src/components/UploadInvoice.tsx` | Existe — DELETE (reemplazado por InvoicePicker) |
| `next.config.js` | Existe (bare) — necesita withPWA wrap en W0.5 |
| `tailwind.config.ts` | Existe |
| `src/app/globals.css` | Existe |
| `.env.example` | Existe — necesita nuevas vars |
| `package.json` | Existe — instalar @ducanh2912/next-pwa en W0.5 |

---

## Convenciones de código

- TypeScript strict — sin `any` explícito (violación = BLOQUEANTE en AR)
- Custom errors Solidity (no `require` strings)
- Zod para validación de env vars
- EIP-712 domain: `{ name: "Cobraya", version: "1", chainId: 43113 }`
- NetworkOnly para `/api/*` y wasiai-a2a/facilitator en service worker (NUNCA cachear payment calls)
- Mock determinista: mismo input → mismo output siempre

---

## Comandos de desarrollo

```bash
npm run dev       # dev server en puerto 3010
npm run build     # build (debe quedar verde por wave)
npm run typecheck # tsc --noEmit (sin build output)
npm run lint      # eslint
npm run seed      # seeding mock data

# Foundry (en contracts/)
forge build
forge test -vv
forge coverage --report summary
forge test --gas-report
forge script script/Deploy.s.sol:DeployCommitments --rpc-url fuji --broadcast --verify -vvv
```

---

## Relación con otros repos (NO modificar durante el hack)

| Repo | Relación |
|---|---|
| `wasiai-a2a` | Consume via REST — /discover, /compose |
| `wasiai-facilitator` | Consume via REST — /settle |
| `wasiai-v2` | Registra agentes via SQL INSERT (agentes quedan en Supabase marketplace) |
| `wasiai-agentshop` | Source pattern — componentes y patrones a copiar/adaptar |

---

## Pipeline NexusAgil

**Modo**: QUALITY AUTO (gates clínicos, no humanos)
**HU activa**: `WKH-COBRAYA-AGENTS`
**Branch**: `feat/wkh-cobraya-agents`
**SDD dir**: `doc/sdd/001-wkh-cobraya-agents/`
